{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {},
  "devDependencies": {
    "lerna": "^7.1.4",
    "nx-cloud": "^16.4.0-beta.1"
  },
  "scripts": {
    "test": "lerna run test",
    "build": "lerna run build"
  }
}
